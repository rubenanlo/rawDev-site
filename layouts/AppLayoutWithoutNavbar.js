const AppLayoutWithoutNavbar = ({ children }) => (
  <div className="bg-gradient-to-r from-gray-900  to-blue-primary font-comfortaa">
    {children}
  </div>
);

export default AppLayoutWithoutNavbar;
